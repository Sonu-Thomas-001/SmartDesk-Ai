version: "3.9"

services:
  smartdesk:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - chroma_data:/app/chroma_data
      - ./feedback_log.jsonl:/app/feedback_log.jsonl
    restart: unless-stopped

volumes:
  chroma_data:
